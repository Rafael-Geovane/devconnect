# Local vhost.conf (workspace root)
# You can copy this into /etc/apache2/sites-available/ or use in Docker
<VirtualHost *:80>
    ServerName example.test
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
